# İleri Seviye Python Örnekleri

Bu repository, Python programlama dilinin ileri seviye konularını içeren örnek kodları barındırmaktadır.

## 📚 İçerik

### 1. Lambda ve Built-in Fonksiyonlar
Python'un güçlü built-in fonksiyonları ve lambda ifadeleri üzerine örnekler:

- **Lambda Fonksiyonları** (`Lambda_func.py`) - Anonim fonksiyon tanımlama
- **Map Fonksiyonu** (`map_func.py`) - Liste elemanlarına fonksiyon uygulama
- **Filter Fonksiyonu** (`filter.py`) - Liste filtreleme işlemleri
- **Any/All** (`any_all.py`) - Mantıksal kontrol fonksiyonları
- **Min/Max** (`min_max.py`) - Minimum ve maksimum değer bulma
- **Sorted** (`sorted_func.py`) - Sıralama işlemleri
- **Sum/Round** (`sum_round.py`) - Toplama ve yuvarlama fonksiyonları

### 2. Nesne Yönelimli Programlama (OOP)
Python'da sınıf yapıları ve OOP prensipleri:

- **Class Attributes** (`class_attributes.py`) - Sınıf nitelikleri
- **Class Constructors** (`class_constructors.py`) - Yapıcı metodlar
- **Class Methods** (`class_methods.py`) - Sınıf metodları
- **Instance Methods** (`instance_methods.py`) - Örnek metodları
- **Kalıtım** (`kalıtım.py`) - Miras alma (inheritance)
- **Child Classes** (`child.py`) - Alt sınıflar
- **Properties** (`properties.py`) - Property dekoratörleri
- **Special Methods** (`special_methods.py`) - Özel metodlar (__init__, __str__, vb.)
- **Meta Classes** (`meta_class.py`) - Meta sınıflar
- **Uygulama** (`uygulama.py`) - Pratik OOP uygulaması

### 3. Python Yineleyicileri (Iterators & Generators)
Iterator ve generator yapıları:

- **Iterable-Iterator** (`iterable-iterator.py`) - Temel iterator kavramları
- **Custom Iterators** (`custom_iterators.py`) - Özel iterator sınıfları
- **Generators** (`generators.py`) - Generator fonksiyonları
- **Generator Uygulaması** (`generator_uygulama.py`) - Pratik generator örnekleri

### 4. Diğer Konular
- **List Comprehension** (`List_comp.py`) - Liste oluşturma teknikleri
- **Variables** (`#variables (değişkenler).py`) - Değişken kullanımı

## 🚀 Kullanım

Her bir Python dosyası bağımsız olarak çalıştırılabilir:

```bash
python "Lambda and Bulit in Functions/Lambda_func.py"
python "Object Orient/class_methods.py"
python "python yineleyicileri/generators.py"
```

## 📋 Gereksinimler

- Python 3.x

## 🎯 Hedef Kitle

Bu repository, Python'un temel seviyesini aşmış ve ileri seviye konuları öğrenmek isteyen geliştiriciler için hazırlanmıştır.

## 📝 Notlar

Her dosya, ilgili konuyu açıklayan yorumlar ve örnek kodlar içermektedir. Dosyaları inceleyerek Python'un ileri seviye özelliklerini öğrenebilirsiniz.

## 🤝 Katkıda Bulunma

Katkılarınızı bekliyoruz! Pull request göndermekten çekinmeyin.

## 📄 Lisans

Bu proje eğitim amaçlıdır ve özgürce kullanılabilir.
